export const dynamic = "force-dynamic";

export const metadata = {
  title: "Kitchen Planner — Cabinet Catalog",
  description: "Design your dream kitchen layout, place real cabinet products, and generate an AI visualization.",
};

export default function PlannerLayout({ children }) {
  return children;
}
